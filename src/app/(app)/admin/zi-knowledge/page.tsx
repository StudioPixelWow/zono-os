import { ZiKnowledgeAdminView } from "./ZiKnowledgeAdminView";

export const dynamic = "force-dynamic";

// ZI Expert™ Knowledge admin (Phase 23) — articles, categories, modules,
// search test, feedback, missing-answer questions + built-in sync. Read-mostly.
export default function ZiKnowledgePage() {
  return <ZiKnowledgeAdminView />;
}
