// ============================================================================
// 🏆 ZONO — Local Market Domination™ Platform page. 34.0.
// Aggregates existing Market + Groups intelligence into a domination strategy.
// No new scoring engine, no new tables; read-only; approval-gated actions.
// ============================================================================
import { DominationDashboard } from "@/components/market-domination/DominationDashboard";

export const dynamic = "force-dynamic";

export default function MarketDominationPage() {
  return <DominationDashboard />;
}
