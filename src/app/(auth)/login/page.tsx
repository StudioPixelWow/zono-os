import { LoginExperience } from "./LoginExperience";

// Premium futuristic ZONO sign-in — a "living real-estate operating system".
export default function LoginPage() {
  return <LoginExperience />;
}
