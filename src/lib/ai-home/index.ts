// ============================================================================
// 🏠 ZONO — Unified AI Workspace™ — barrel. 30.2.
// UX orchestration over existing engines (read-only). No engine modified; no
// business logic duplicated; everything approval-gated; nothing auto-executes.
// ============================================================================
export { buildAiHome, buildContextPanel, bandOf } from "./assemble";
export { runSelfCheck } from "./qa";
export { getAiHome } from "./service";
export * from "./types";
