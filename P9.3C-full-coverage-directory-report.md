# P9.3C — FULL COVERAGE DIRECTORY REPORT

Moving from method-proof to full coverage. No Apify · no System-B rebuild · no Claim · no Grow/billing · no fabrication · no invented pagination.

## HEADLINE (honest)

- **MULTI_CITY_OFFICE_PRESENCE_MODEL = PASS — MIGRATION = NO** (proven this turn).
- **FULL-COVERAGE WEB CRAWLER = NO-GO from this environment** — not because Madlan is blocked (it's reachable), and not a refusal to build, but a **tooling boundary**: the only sanctioned web tool here (WebFetch) is capped and cannot see or reach Madlan's directory API, and I cannot discover that API's contract from this sandbox nor invent it.

## Why full coverage cannot be produced here (evidence, this turn)

1. **WebFetch caps at ~40 and cannot paginate.** Live probe on Haifa asking for offices 41–100 returned verbatim: *"only first ~40 present; remainder loaded dynamically."* The other ~250 Haifa offices are loaded client-side by the SPA — WebFetch only ever sees the initial ~40.
2. **The directory API is not discoverable from static content.** Live probe for endpoint/hydration returned: *"no API endpoint or hydration state visible in static content"* — the office list is fetched dynamically after JS init. So I cannot read the real pagination contract (page/cursor/limit/total) from the page.
3. **I cannot route around WebFetch.** My operating rules prohibit fetching web content via curl/wget/Node/Python/any HTTP library — so I cannot run a direct-HTTP crawler against Madlan from this container.
4. **I will not invent the contract.** §2 forbids inventing pagination; §25 forbids bypass. Discovering the real contract would require observing the SPA's runtime network calls (a browser session against Madlan) — which is the customer-impersonation/anti-bot-sensitive path the constraints bar.

**This is a tooling boundary, not `SOURCE_BLOCKED`.** Madlan responds fine; the gap is that my sandbox has no sanctioned mechanism for full paginated crawling, and the SPA hides its API from the static read.

## Per-city (current persisted vs live source)

| City | SOURCE_REPORTED | PERSISTED (canonical) | COVERAGE % | PROFILES ✓/✗ | AGENTS | RELATIONSHIPS | STATUS |
|---|---|---|---|---|---|---|---|
| רחובות (control) | 69 | 42 | ~61% | 6 / 1 | 60 | 30 | PARTIAL |
| נתניה | 117 | 20 | ~17% | 1 / 0 | 7 | 7 | PARTIAL |
| חיפה | 290 | 20 | ~7% | 1 / 0 | 4 | 4 | PARTIAL |
| ראשון לציון | 113 | 18 | ~16% | 1 / 0 | 3 | 3 | PARTIAL |
| פתח תקווה | — | 0 | — | — | — | — | FAILED / RETRY_LATER (WebFetch provenance — tool, not Madlan) |

No city reached COMPLETE — capped by the WebFetch ~40 limit, not by source exhaustion.

## §10 Rehovot delta (69 source vs 42 persisted) — explained

- **42 persisted** = WebFetch page-1 (~40 offices) + LANDSMAN + Weshare (surfaced via agent-profile links). 
- **27 missing** classified: **NOT_YET_IMPORTED** — they sit beyond WebFetch's ~40 presentation cap and load dynamically. **Not** DUPLICATE_CANONICAL (0 dup rows), **not** SOURCE_CHANGED (control re-fetch still shows 69), **not** PROFILE_UNAVAILABLE. Purely the tool cap. They import the moment a paginating crawler runs.

## §8 Multi-city presence — PASS, NO migration (proven)

Existing `brokerage_offices.metadata` (jsonb) represents multi-city presence cleanly: one canonical office (keyed by `madlan_entity_id`) + a `directory_cities` array, without duplicating the office or overwriting its canonical/HQ city. Demonstrated live: **פרו בונה** and **אור נכסים** (listed in both Rehovot and Rishon) now hold `directory_cities = ["רחובות","ראשון לציון"]` on a single row. The crawler must **append** city-presence on each city observation (dedupe) instead of skipping the second city — a code behavior, **no schema change**.

## Flags

- **HAIFA_FULL_COVERAGE = FAIL** (7% via WebFetch; the ~40 cap makes 290 unreachable here).
- **PETAH_TIKVA = FAILED (tool boundary)** — WebFetch provenance refusal, not `SOURCE_BLOCKED`; a real crawler resolves it.
- **MULTI_CITY_OFFICE_PRESENCE_MODEL = PASS** · **MIGRATION = NO**.
- **RESUME / RATE_SAFETY = N/A** — the crawler wasn't executed (blocked above); the design (chunked, cursor-resumable, hard time-budget, backoff) is specified and matches the P9.3 cron's budget model, ready to wire once the contract exists.
- **IDEMPOTENCY = PASS** · **SYSTEM_B = PASS** · **TERRITORY_ISOLATION = PASS** (all from the persisted 4-city data; RUN #2 = 0 dups earlier).
- **CLAIM_IDENTITY_ANCHORS = 29** (count only; Claim unbuilt).

## The exact unblock (no Apify, no invention) — minimum external action

Full coverage needs Madlan's real directory API contract, which I can't capture from here. One small step unblocks it:

1. **Capture the contract once** (≈5 min, any ZONO dev): open a proven city's `/madad-search/{city}-ישראל` in a browser → DevTools → Network → filter XHR/Fetch → scroll to load more offices → copy the directory request (URL/endpoint, method, the pagination params — page/offset/cursor/limit, and the response JSON shape incl. `total`/`hasNextPage`).
2. **Give me that real contract.** I then build a **ZONO server crawler** (a background route/cron using normal `fetch()` to Madlan's public API — the sanctioned HTTP you asked for) that: paginates to source-exhaustion, crawls every office profile, persists with the multi-city `directory_cities` model + idempotency, is cursor-resumable and chunked under a hard time budget (no 300s SIGKILL), backs off on 429/5xx, and reconciles `SOURCE_REPORTED vs UNIQUE vs COVERAGE%` per city. It runs full coverage in production — Haifa 290 included.

No contract is invented — you supply the real one from your own browser; I build against it. (This is the same capture the earlier actor's discovery mode would have done; you've excluded Apify, so the one-time manual capture replaces it.)

---

## FINAL VERDICT

- **FULL-COVERAGE WEB CRAWLER = NO-GO** (this environment) — WebFetch is capped and can't see/reach the SPA's API; direct-HTTP crawling isn't available to me; the API contract is undiscoverable from here and I won't invent it. Unblock = supply the captured contract → I build the ZONO server crawler.
- **READY_FOR_25_CITY_TRANCHE = NO** — gate requires ≥4 cities COMPLETE with source-total reconciliation + Haifa COMPLETE; none reached COMPLETE (tool cap).
- **MIGRATION = NO** — multi-city presence fits existing schema (proven).

**Stopped. No 25-city crawl · no Claim · Grow/billing untouched · System-B intact · no fabricated records · no invented pagination · no Apify proposed.**
