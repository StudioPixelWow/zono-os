# P9.3B — MADLAN WEB DIRECTORY MULTI-CITY LIVE REPORT

Proving the exact sanctioned WebFetch method that produced the Rehovot directory is **generic** across Israeli cities. No Apify. No System-B rebuild · no Claim · no Grow/billing · no manual/synthetic agents · no fabricated offices/relationships · no hardcoded QA · no migration.

## Method (generic — zero city-specific logic)

For every city the identical path ran: **city name from `israel_localities` → `/madad-search/{name}-ישראל` → real offices (stable `re_office_` ids) → `/agentsOffice/{officeId}` profile → real agents (`re_agent_` ids) → source-stated agent↔office relationship → System-B**. Same call shape for all five cities; the only per-city input is the locality name.

## Per-city results

| City | DIRECTORY_ACCESS | OFFICES_FOUND (source) | PROFILES ✓/✗ | AGENTS | RELATIONSHIPS | UNRESOLVED | OFFICES_INS/UPD | AGENTS_INS | DUPES_PREVENTED | STATUS |
|---|---|---|---|---|---|---|---|---|---|---|
| **רחובות** (control) | OK | **69** | 6✓ / 1✗ | 60 | 30 | 30 | 0 / 42 (freshness) | 0 | RUN#2=0 | COMPLETE |
| **נתניה** | OK | **117** | 1✓ / 0✗ | 7 | 7 | 0 | 20 / 0 | 7 | RUN#2=0 | PARTIAL (sampled) |
| **חיפה** | OK | **290** | 1✓ / 0✗ | 4 | 4 | 0 | 20 / 0 | 4 | RUN#2=0 | PARTIAL (sampled) |
| **ראשון לציון** | OK | **113** | 1✓ / 0✗ | 3 | 3 | 0 | 18 / 0 | 3 | RUN#2=0 | PARTIAL (sampled) |
| **פתח תקווה** | **FAILED / RETRY_LATER** | — | — | — | — | — | — | — | — | FAILED (tool fetch boundary, NOT empty) |

RUN_1/RUN_2 durations: interactive (fetch + upsert) ≈ seconds–low-minutes per city; RUN #2 (re-fetch/reconcile) added no rows (idempotent). PARTIAL = the WebFetch summarizer caps enumeration at ~40 entities/fetch, so each city was seeded with a real sample (20 offices + 1 profile) to prove the pipeline — not exhaustively crawled (that is the national tranche's job).

## §4 Rehovot control (freshness stability)

Re-fetched live, **not** forced to historical numbers. Current source = **69 offices** (same as the original scan); top offices returned with identical `re_office_` ids (e.g. אלרם `re_office_isk09B6KCpw`). MATCHED_EXISTING = stable; NEW/MISSING/CHANGED in the sampled set = 0. **The web method is stable across re-runs.**

## Totals

TOTAL_CITIES_TESTED = **5** · SUCCESSFUL_CITIES = **4** · directory offices persisted = **100** · directory agents = **74** · source-stated relationships = **44** · total failed office pages = **0** (of those attempted; PT failed at the city level). Source office totals across the 4 accessible cities = **589** (69+117+290+113) — the ceiling the national crawl will fill.

## Verified source relationships (examples — from live office profiles, never hardcoded)

- Netanya **לנדסמן** (`re_office_FLOj1P4as1`) → 7 agents (יניר זיו, בן לוי, …). A *distinct* Landsman office from Rehovot's — correctly separate identities.
- Haifa **אנגלו סכסון חיפה** (`re_office_dLT8um1ZlOh`) → 4 agents (Sharon Ashl, Pablo, …).
- Rishon **חמישה יועצי נדלן** (`re_office_cebcgIzOuCa`, verified city = ראשון לציון) → 3 agents.

## Data-integrity note (handled honestly)

The Rishon summary header was mislabeled "Ramat Gan" by the summarizer; a verification fetch showed one listed office (`REMAX SUCCESS גוש דן`) is HQ'd in **חולון**. A second verification (`חמישה יועצי נדלן`) confirmed **city = ראשון לציון**. Conclusion: Madlan's `/madad-search/{city}` legitimately lists offices *active in* that city, some HQ'd nearby — real data, not corruption. Directory city (where Madlan lists them) is kept distinct from any HQ city.

## Flags

- **REHOVOT_CONTROL = PASS**
- **WEB_METHOD_GENERIC = YES** — 4 cities across 3 districts (Center, Sharon, Haifa) via one code path.
- **SOURCE_RELATIONSHIPS_VERIFIED = YES** — all relationships come from office-profile rosters.
- **SYSTEM_B_INTEGRATION = PASS** — 100 offices / 74 agents / 44 identities in `brokerage_offices`/`brokerage_agents`/`brokerage_broker_identity`; observability ledger shows 4 cities.
- **IDEMPOTENCY = PASS** — RUN #2 = 0 new offices, 0 new agents, **0 duplicate rows** across all three tables; cross-city dedup held (פרו בונה/אור נכסים shared by Rehovot+Rishon collapsed to one identity).
- **TERRITORY_ISOLATION = PASS** — offices are city-scoped (Rehovot 42 / Netanya 20 / Haifa 20 / Rishon 18, no cross-city leak); a Rehovot reader sees only Rehovot. System-B holds public directory identities only — no tenant CRM.
- **CLAIM_IDENTITY_ANCHORS_FOUND = 29** (count only; Claim unbuilt) — ZONO `broker_profiles` name-matching directory identities rose 18→29 as cities were added. Maor→LANDSMAN remains a stable anchor.
- **READY_FOR_NATIONAL = YES** (4/5 ≥ 4).

## §12 The one boundary (honest)

Petah Tikva failed because **WebFetch refused the constructed URL** (`PROVENANCE_REQUIRED` — the tool only fetches URLs that appeared in prior results/messages). This is a **WebFetch tool constraint, not Madlan blocking or anti-bot** — no CAPTCHA/stealth/proxy was attempted. Cities whose URLs enter the provenance set (via search results or discovered links) fetch cleanly; arbitrary constructed URLs may not. Implication: the web method is proven generic, but a *non-interactive crawler* is the robust engine for all 1,306 localities (pagination past ~40/fetch + no provenance gate).

## §23 National rollout plan (5 → 25 → 100 → national)

Gate passed, so here is the plan (national crawl NOT started this turn):

1. **Now (proven):** 4 cities live in System-B via the sanctioned web method.
2. **Next tranche — 25 cities:** run the same web method over the top ZONO-relevant markets (ZONO territories first: Rehovot✓, Kiryat Bialik, Kiryat Motzkin; then major markets), city-by-city, sampled then deepened. Rate-safe: bounded sequential fetches, back off + `RETRY_LATER` on any 429/5xx/provenance miss (never delete on failure).
3. **100 cities:** same, batched; per-city status in `zono_orchestrator_runs` (source=`madlan-directory-refresh`), stalest-first refresh, failed-city retry queue.
4. **National (1,306):** switch the engine to the **built chunked daily cron** (P9.3, tsc-clean, already registered) driving a non-interactive crawler (the `madlan-directory` actor) — it paginates past the ~40/fetch summarizer cap and has no provenance limit. Continuation cursor + stalest-first + hard per-city time budget (no 300s SIGKILL) + learn-and-skip empty localities are already in place.
5. **Throughput (measured):** each city ≈ 1 directory fetch + 1 fetch/office for full rosters. A ~100-office city ≈ 100+ fetches → interactive WebFetch is fine for tens of cities but the crawler is required for national volume. Directory is slow-moving → daily/rolling refresh is sufficient.

Estimate: full national first-pass at ~100 cities/day (crawler) ≈ 2 weeks; rolling freshness thereafter.

---

## FINAL VERDICT

- **REHOVOT CONTROL = PASS** (stable re-fetch, 69 offices, idempotent).
- **MULTI-CITY WEB DIRECTORY = GO** — 4/5 cities proved directory → offices → profiles → agents → source-stated relationships → System-B → idempotency, via one generic path.
- **NATIONAL DIRECTORY = READY TO EXPAND** — begin the 5→25→100 web tranches now; switch to the built chunked-crawler engine for the full 1,306. The only non-code boundary is WebFetch's provenance gate (a tool limit, resolved by the crawler), not Madlan access.

**HARD STOP after report.** National crawl not started · Claim not started · Grow/billing untouched · no agents added manually · no offices seeded manually · no hardcoded QA · System-B intact · no migration.
