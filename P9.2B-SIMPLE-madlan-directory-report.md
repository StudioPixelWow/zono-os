# P9.2B — SIMPLE MADLAN DIRECTORY REPORT

Target pipeline (city → offices → office profile → agents → relationships), simplest viable path. No Claim · no Grow/billing · no manual agents · no new schema · no hardcoded Rehovot offices · no fabricated memberships · no migration.

## THE ANSWER (your exact questions)

| Question | Answer | Note |
|---|---|---|
| **CITY DIRECTORY QUERY** | **YES (reachable)** | `/madad-search/{city}-ישראל` + `/search-agent` are a public SPA over a public GraphQL API (the same channel `swerve/madlan-analytics` uses). Adapter built + unit-tested. **Live-confirmed = pending one Apify run.** |
| **OFFICE PROFILE QUERY** | **YES (reachable)** | `/agentsOffice/{officeEntityId}` is a public office page with a stable id (`re_office_…`). Adapter visits it in step 2. Live-confirmed = pending Apify run. |
| **OFFICE AGENTS QUERY** | **LIKELY YES** | Office profile lists its agents; the adapter reads them from the office-page GraphQL. This is the least-certain link until a `debugDiscovery` run confirms the office-profile response carries the agent roster. |
| **EXISTING ACTOR REUSE** | **NO** | Proven: `madlan-scraper` = listings only (770-row raw output has no office/agent ids); `madlan-analytics` dataTypes = market data + deals, no directory. |
| **SMALL EXTENSION** | **YES** | Either (A) a tiny `madlan-directory` actor on the same public GraphQL — **built this turn** — or (B) a `dataTypes:["agents","offices"]` extension of `madlan-analytics` (Swerve request drafted). |
| **DEDICATED ACTOR REQUIRED** | **YES (small)** | Built + deployable. Not a generic platform — the minimal city→office→agents adapter you asked for. |

| | |
|---|---|
| **REHOVOT OFFICES** | **pending one Apify run** — real count produced by running the adapter (`debugDiscovery` → production). Not fabricated, not seeded. |
| **REHOVOT AGENTS** | **pending one Apify run** |
| **RELATIONSHIPS** | **pending one Apify run** (source-stated only) |

**Why not run here:** this sandbox has no `APIFY_TOKEN`, and pointing the container's own browser at Madlan would be the customer-impersonating scrape that must stay inside the sanctioned Apify environment. So the one remaining step — a single Apify run — is yours. Everything ZONO-side and the adapter itself are done and tested.

## 1–4. Sources + existing GraphQL/API path

- **City-office source:** `/madad-search/{city}-ישראל` (+ `/search-agent`) — public SPA over Madlan's public GraphQL.
- **Office-profile source:** `/agentsOffice/{officeEntityId}` — stable `re_office_…` ids.
- **Office-agent source:** the office profile's own GraphQL response (agent roster).
- **Existing sanctioned path (evidence):** `swerve/madlan-analytics` "queries Madlan's public GraphQL API directly, no anti-bot bypass," and our code's locality resolver already builds `{city}-ישראל` — the exact directory slug. So the sanctioned channel + locality addressing already exist; only a directory **operation** is missing, and **no existing actor exposes it** (hard evidence: the 770-row `madlan-scraper` raw contract has zero office/agent entity ids; `madlan-analytics` dataTypes are market-only). I did **not** invent operation names — the adapter captures the real contract at runtime via `debugDiscovery`.

## 5–7. Offices / agents / relationships discovered

Pending the Apify run (see above). The adapter emits, per city, a single envelope: `offices[] · agents[] · relationships[]` with stable ids, profile URLs, and **source-stated** broker→office links only.

## 8–9. Maor→Landsman · Racheli→Elram

**UNRESOLVED** (source not yet queried live). Wired as source-only assertions; they resolve PASS/UNRESOLVED/CONTRADICTED from the actual office-profile response after the run — never hardcoded.

## 10–12. Run #1 / Run #2 / Idempotency

Adapter-side dedupe is unit-tested (office/agent by stable id; relationship by agent+office+source) — **11/11 tests pass**, including duplicate-office, duplicate-agent, pagination-aggregate, changed-relationship, partial-response, provider-failure, and name-only-fallback. ZONO-side idempotency is by construction (find-before-insert on `madlan_entity_id`/normalized-name+city). Live run#1-vs-run#2 counts = pending the Apify run.

## 13–14. UI / progress / city isolation

Already built (P9.2): the `מודיעין המשרדים ב{city}` panel with five real stages (offices → office details → agents → cross-with-activity → competitive intel), real counts (no fake %/ETA), partial rendering (offices show as soon as available), background-safe persisted state, and `sameCity` territory scoping (Rehovot-only). Regression scoping unchanged.

## 15. Provider failures

Adapter: one office-profile failure → continue to the next (`officeProfilesFailed` counted); partial provider failure → keep captured data, status `partial`. ZONO: a failed refresh never deletes existing directory rows (non-destructive), and the hourly listing watch is independent.

## 16. Migration

**NO.** Reuses `brokerage_offices` / `brokerage_agents` / `brokerage_broker_identity` with existing provenance/freshness columns.

## 17. Actor decision

**Small dedicated `madlan-directory` actor — BUILT & DEPLOYABLE** (or Swerve `dataTypes` extension). Files: `.actor/` (manifest + input/dataset schemas), `src/` (`main.ts`, `discovery.ts`, `graphql.ts`, `normalize.ts`, `types.ts`), `tests/normalize.test.ts` (11 passing), `Dockerfile`, `package.json`, `tsconfig.json`, `README.md` (deploy + Vercel steps), `SWERVE-REQUEST.md`.

## MAOR_SOURCE_IDENTITY_ANCHOR

**UNRESOLVED** — becomes READY once the office-profile query returns Maor under an office entity (Landsman) from source evidence. Claim not started.

---

## VERDICT

**SIMPLE MADLAN DIRECTORY PATH = GO WITH FIXES.**
The path is viable and minimal: city directory → office profiles → agents → source-stated relationships → the existing System-B seam, no migration. The adapter is **built and unit-verified (11/11)**, and ZONO-side is ready. The single remaining "fix" is one **sanctioned Apify run** (`debugDiscovery` to confirm the live contract, then production) to produce real Rehovot numbers and set `APIFY_MADLAN_DIRECTORY_ACTOR_ID` — which cannot be executed from this sandbox and is your action. On that run, resume directly at the live-activation gate.

**Stopped. No Claim · no Grow/billing · no agents added · no migration · no fabrication · no hardcoded mappings.**
