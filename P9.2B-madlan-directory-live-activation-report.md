# P9.2B — MADLAN DIRECTORY LIVE ACTIVATION REPORT

Focused closure phase. Architecture accepted/unchanged · System-B not rebuilt · no migration · no Claim work · Grow/billing untouched · no manual agents · **no fabricated office/broker records**. The P9.2A-HD hourly market-watch gate was not touched or re-marked.

## HEADLINE

**VERDICT: MADLAN DIRECTORY PROVIDER = PROVIDER_BLOCKED (environment access boundary, not a design gap).**
**OFFICE INTELLIGENCE LIVE DATA = NO-GO (pending provisioning — one external action).**

The already-built foundation is correct and ready. It cannot be flipped to live **from this build environment** because provisioning a sanctioned Apify actor requires Apify account/credentials/deploy access that does not exist here. Per Section 23 I STOP and report the exact reason + the minimum external action — I did **not** fall back to inference, AI lists, Google, manual DB seeding, or hardcoded offices.

## 1–2. Provider / source-contract investigation (sanctioned, public-page only)

Re-confirmed without repeating the store audit, using only public-page inspection (no anti-bot bypass, no scraping, no fake-customer browser):

- Directory entry point is **`/madad-search/{city}-ישראל`** (e.g. `/madad-search/רחובות-ישראל`) plus `/search-agent` ("מאגר המתווכים") and office profile pages `/agentsOffice/{officeEntityId}` (stable id form `re_office_…`).
- The directory is a **client-side SPA** ("You need to enable JavaScript to run this app") backed by a **private JSON/GraphQL API**. The `swerve/madlan-analytics` actor already speaks Madlan's public GraphQL — so an **API-based directory actor is feasible** on the Apify side.
- **The exact directory query/response contract is not publicly documented** and cannot be captured from a static fetch — it requires executing the SPA and observing its API calls. The legitimate place to do that is **inside the sanctioned Apify actor during development**, using Apify's own tooling — not from ZONO app code and not via a customer-impersonating browser (both explicitly forbidden). I therefore did **not** invent a GraphQL request/response shape (Section 2: "NO fabricated response structures").

## 3. Actor build/config — WHY IT CANNOT HAPPEN HERE (exact technical reason)

Provisioning the smallest dedicated Madlan-directory actor requires ALL of the following, none of which exist in this cloud build environment:

1. **An Apify account + `APIFY_TOKEN`** — unset in this container (verified: `APIFY_TOKEN`, `APIFY_MADLAN_DIRECTORY_ACTOR_ID` both absent). No Apify MCP/tool is connected to this session.
2. **An Apify deploy path** (`apify push` / Apify console) to create and publish the actor — not available here.
3. **A way to capture the live directory API contract** (SPA execution) inside that sanctioned actor — impossible without (1)/(2).
4. **Vercel environment write access** to set `APIFY_MADLAN_DIRECTORY_ACTOR_ID` — the connected Vercel tooling here exposes deploy/read, **not** environment-variable management.

This is an access boundary. It is not solvable by more ZONO code, and the rules bar every non-sanctioned shortcut.

## 4. Deployment SHA

Foundation deployed earlier from `main` (push `c175848..078b0c8`). **No new deploy this phase** — no code needed changing; the block is provisioning, not code.

## 5–11. Rehovot run #1 / run #2 / counts / duplicates

Not executable — no directory actor to run through the seam. Honest live state (unchanged, no fabrication): Rehovot `brokerage_offices` = **0**, `brokerage_agents` = **0**, `metadata.source='madlan_directory'` rows = **0**, `resolution_method='madlan_directory'` = **0**, `madlan-directory-refresh` run rows = **0**. Idempotency remains proven **by construction** (source-id / normalized-name+city keys, in-run `seenKeys`, find-before-insert, identity keyed by `agent_id`); the live twice-run proof is pending the actor.

## 12–15. Maor→Landsman · Racheli→Elram · Eliran · Eyal Landsman

All **UNRESOLVED** — assertions are wired against the live source and will resolve from source evidence once the actor runs. Not hardcoded, not faked PASS.

## 16. Directory vs activity (live examples)

Pending real data. The separation is implemented and verified structurally (`computeDirectoryActivity`: presence vs phone-joined observed-active; "active" computed, never a status mutation).

## 17. Office Intelligence UI proof

The `DirectoryStatusPanel` renders live on `מדריך המשרדים` for the selected/first operating city. With no source wired it currently shows the honest "מקור המדריך אינו מחובר עדיין" state (no fabricated cards). It flips to populated office cards automatically on the first successful run.

## 18. Broker Intelligence cross-link proof

Pending real System-B directory identities (the cross-links read the same `brokerage_agents.office_id` / `brokerage_broker_identity` the seeder populates).

## 19. Progress UX / 20. Background execution / 21. Provider-failure preservation / 22. City isolation

Implemented and verified by construction in P9.2 (five real stages, no fake %/ETA; background-safe persisted-state reads; non-destructive failure path; `sameCity` territory scoping — Rehovot-only, no קריית ביאליק/חיפה/קריות leak). Live end-to-end proof of each is pending real data.

## 23. Observability proof

`zono_orchestrator_runs` filtered to `source='madlan-directory-refresh'` currently returns **0 rows** (no fabricated runs). The lifecycle/stale-recovery/metadata wiring is in place; real rows appear on the first run.

## 24. CLAIM_IDENTITY_ANCHOR status

**CLAIM_IDENTITY_ANCHOR = UNRESOLVED** — Maor has no external source identity anchor yet because the Madlan agent/office entities do not exist in System-B until the directory runs. (Claim My Listings NOT started.)

## 25. Build / regression

No code changed this phase; last state green (`tsc` 0, `eslint` clean, `next build` exit 0, `use-server` gate clean, directory cron compiled).

## 26. Migration

**NO.**

## 27. Remaining blockers — THE MINIMUM EXTERNAL ACTION

One blocker, closable outside this environment. To make Rehovot live:

1. **Provision the actor (Apify side).** Create the smallest dedicated **Madlan-directory** Apify actor. Recommended approach (matching the sanctioned `madlan-analytics` pattern): an **API-integration actor** that calls Madlan's public directory GraphQL for `/madad-search/{locality}` and office/agent profiles; capture the real request/response contract during first-run development inside Apify (respect rate limits; no anti-bot bypass). Emit dataset items matching the **already-implemented `CityDirectoryFetch` normalizer** — either a flat typed list or an envelope `{offices[],agents[],relationships[]}`, using keys the seeder already reads:
   - office: `id`/`officeId` (→ `re_office_…`), `name`/`officeName`, `url`/`profileUrl`, `city`, `phone`, `address`, `website`, `brand`.
   - agent: `id`/`agentId`, `name`/`agentName`, `url`/`profileUrl`, `city`, `phone`, `role`, `officeId` (the source-stated office).
   Capture **stable source entity IDs** (Section 4) — never display-name alone as identity.
2. **Set env (Vercel).** `APIFY_MADLAN_DIRECTORY_ACTOR_ID=<owner>/<actor>` and confirm `APIFY_TOKEN` is present. Redeploy. (No default is intentional — the pipeline must never fall back to a listings actor.)
3. **Run Rehovot.** The daily cron fires at `30 3` UTC, or hit **"רענן מדריך"** on `מדריך המשרדים`, or `curl` the cron with `CRON_SECRET`. Real offices/brokers/relationships then flow through the existing seam — zero further ZONO code.

**If you can grant this session sanctioned Apify access** (an `APIFY_TOKEN` + a deployed directory actor id, or an Apify MCP connection), I can immediately run Rehovot live through the seam and return the real report items 5–24. Absent that access, this is the honest stopping point.

---

## VERDICT

- **MADLAN DIRECTORY PROVIDER = PROVIDER_BLOCKED** — sanctioned Apify provisioning/deploy access and the live directory API contract cannot be obtained from this build environment; blocked cleanly, no forbidden fallback used.
- **OFFICE INTELLIGENCE LIVE DATA = NO-GO** — pending the single external provisioning action above; the foundation is ready to light up with zero code change.

**Stopped after report. No fabrication · no migration · no Claim work · no agents · Grow/billing untouched · hourly gate untouched.**
